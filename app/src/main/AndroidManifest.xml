<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vicky.courtside">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gs" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
           <activity android:name=".Login"
           class=".Login">
        -->
        <activity
            android:name=".signup"
            class=".signup" />
        <activity
            android:name=".user"
            android:label="@string/title_activity_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Login" />
        <activity android:name=".football" />
        <activity android:name=".xlr" />
        <activity android:name=".booking" />
        <activity android:name=".Powerplay" />
        <activity android:name=".Cancel" />
        <activity android:name=".letsplay" />
        <activity android:name=".Matchday" />
        <activity android:name=".cricket" />
        <activity android:name=".Enlivensports" />
        <activity android:name=".Magniumsports" />
        <activity android:name=".Msground" />
        <activity android:name=".unitedsports" />
        <activity android:name=".tennis" />
        <activity android:name=".bulldog" />
        <activity android:name=".kinesis" />
        <activity android:name=".noah" />
        <activity android:name=".topspin" />
        <activity android:name=".badminton" />
        <activity android:name=".argon" />
        <activity android:name=".Ftouch" />
        <activity android:name=".namma" />
        <activity android:name=".playmania"></activity>
    </application>

</manifest>